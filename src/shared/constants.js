export const MAX_USER_CACHE = 30; // Should be multiplier of limit
export const RESET_USER_CACHE = 15; // Minimum users stored in user.data;
export const USER_PREFETCH_LIMIT = 5;

export const USER_PREFERENCE_TYPES = {
  LIKE: 'like',
  PASS: 'pass',
};